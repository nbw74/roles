---
- name: create multisite dirs
  command: "/usr/local/bin/{{ bxmult_sh }} {{ item.domain }}"
  args:
    chdir: "/home/bitrix"
    creates: "{{ item.domain }}"
  when: not item.adaptive|d(true)
  with_items: "{{ bxvhost.multisite|d([]) }}"
  tags: [ multisite ]
...
