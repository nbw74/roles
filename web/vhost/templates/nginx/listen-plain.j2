{% if op.listen is defined %}
{% for home in op.listen %}
{% if home.ipaddr == 'all' %}
    listen		{{ home.port|d('80') }};
{% else %}
    listen		{{ home.ipaddr }}:{{ home.port|d('80') }};
{% endif %}
{% endfor %}
{% else %}
    listen		{{ vhost_default_ipaddr }}:80;
{% endif %}

