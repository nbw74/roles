/* THIS FILE IS MANAGED BY ANSIBLE, ALL CHANGES WILL BE LOST */
{% if item.crypto|d() %}
    vars.www_tls["{{ item.name }}"] = {
{% else %}
    vars.www["{{ item.name }}"] = {
{% endif %}
        http_string = "{{ item.content }}"
{% if item.url is defined %}
        http_url = "{{ item.url }}"
{% endif %}
{% if item.notify is defined %}
        notification_entity = "{{ item.notify }}"
{% endif %}
    }
/* EOF */
