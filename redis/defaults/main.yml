---
redis_service_enabled: true
# Set to an empty list to disable persistence (saving the DB to disk).
redis_save:
  - 900 1
  - 300 10
  - 60 10000

redis_conf: { }
redis_includes: []

redis_pcmk_ra_org: local
redis_pcmk_vip: { }
...
