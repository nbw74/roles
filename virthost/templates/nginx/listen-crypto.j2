{% if item.listen is defined %}
{% for home in item.listen %}
    listen		{{ home.ipaddr }}:{{ home.portcrypto|d('443') }} ssl http2;
{% endfor %}
{% else %}
    listen		{{ vhost_default_ipaddr }}:443 ssl http2;
{% endif %}

